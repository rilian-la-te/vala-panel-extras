include(GSettings)
if (ENABLE_ALSA)
    add_schema("org.valapanel.volume.gschema.xml")
endif()
if (ENABLE_WEATHER)
    add_schema("org.valapanel.weather.gschema.xml")
endif()
if (ENABLE_XKB)
    add_schema("org.valapanel.xkb.gschema.xml")
endif()
if (ENABLE_BATTERY)
    add_schema("org.valapanel.battery.gschema.xml")
endif()
if (ENABLE_LIBGTOP)
    add_schema("org.valapanel.gtop.gschema.xml")
endif()
